server:
  port: 9527
  servlet:
    context-path: /ylist
spring:
  thymeleaf:
    mode: HTML
    encoding: UTF-8
    servlet:
      content-type: text/html
    cache: false

rootPath: /home
userfile: userlist.ini